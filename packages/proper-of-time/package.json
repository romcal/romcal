{
  "name": "@internal/proper-of-time",
  "version": "0.0.0",
  "description": "Shared liturgical anchors (Easter, Advent, etc.) and date helpers used by every rite.",
  "license": "MIT",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "tsc --project tsconfig.release.json",
    "clean": "rimraf coverage dist",
    "prettier": "prettier -c .",
    "test": "TZ=UTC jest"
  },
  "dependencies": {
    "@internal/constants": "file:../../packages/constants",
    "@internal/easter": "file:../../packages/easter"
  },
  "devDependencies": {
    "@internal/config": "file:../../packages/config"
  }
}
