{
  "name": "@internal/i18n",
  "version": "0.0.0",
  "description": "Shared i18next helpers used by every rite (instance factory + bundle loader).",
  "license": "MIT",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "tsc --project tsconfig.release.json",
    "clean": "rimraf coverage dist",
    "prettier": "prettier -c ."
  },
  "dependencies": {
    "i18next": "^26.0.3"
  },
  "devDependencies": {
    "@internal/config": "file:../../packages/config"
  }
}
