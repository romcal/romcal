{
  "name": "@internal/config",
  "version": "0.0.0",
  "description": "Configuration files for the Romcal project",
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "npm run lint -- --fix",
    "prettier": "prettier -c ."
  },
  "devDependencies": {
    "jest": "^30.3.0",
    "jest-extended": "^7.0.0"
  },
  "license": "MIT"
}
