{
  "name": "@internal/easter",
  "version": "0.0.0",
  "description": "Calculate the date of Easter in both the Gregorian and Julian calendars.",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "tsc --project tsconfig.release.json",
    "clean": "rimraf coverage dist",
    "prettier": "prettier -c .",
    "test": "TZ=UTC jest"
  },
  "dependencies": {},
  "devDependencies": {
    "@internal/config": "file:../../packages/config"
  },
  "license": "MIT"
}
