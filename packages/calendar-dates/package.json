{
  "name": "@internal/calendar-dates",
  "version": "0.0.0",
  "description": "Date primitives and the liturgical year anchors shared by every Roman Rite calendar.",
  "license": "MIT",
  "main": "src/index.ts",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.release.json",
    "clean": "rimraf coverage dist",
    "test": "TZ=UTC jest",
    "test:without-coverage": "TZ=UTC jest",
    "typecheck": "tsc --noEmit --importHelpers false -p tsconfig.json"
  },
  "dependencies": {
    "@internal/easter": "file:../easter",
    "@internal/lunar-new-year": "file:../lunar-new-year"
  }
}
