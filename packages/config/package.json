{
  "name": "@internal/config",
  "version": "0.0.0",
  "description": "Configuration files for the Romcal project",
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "npm run lint -- --fix",
    "prettier": "prettier -c ."
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-extended": "^4.0.2"
  },
  "license": "MIT"
}
