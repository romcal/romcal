{
  "name": "@internal/constants",
  "version": "0.0.0",
  "description": "Calendar-neutral constants (Months, Weekdays, DayOfWeek) used by every rite.",
  "license": "MIT",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "tsc --project tsconfig.release.json",
    "clean": "rimraf coverage dist",
    "prettier": "prettier -c ."
  },
  "devDependencies": {
    "@internal/config": "file:../../packages/config"
  }
}
