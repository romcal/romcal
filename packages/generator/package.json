{
  "name": "@internal/generator",
  "version": "0.0.0",
  "description": "The calendar engine shared by every Roman Rite: contracts, the build pipeline and the Proper of Time.",
  "license": "MIT",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "tsc --project tsconfig.release.json",
    "clean": "rimraf coverage dist",
    "test": "TZ=UTC jest --passWithNoTests"
  },
  "dependencies": {
    "@internal/proper-of-time": "file:../proper-of-time",
    "i18next": "^26.4.0",
    "ts-deepmerge": "^8.0.0"
  }
}
