{
  "name": "@internal/config",
  "version": "0.0.0",
  "description": "Configuration files for the Romcal project",
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "npm run lint -- --fix",
    "prettier": "prettier -c ."
  },
  "devDependencies": {
    "jest": "^30.0.5",
    "jest-extended": "^6.0.0"
  },
  "license": "MIT"
}
